import 'package:flutter/material.dart';

class HomeScrollableContainer extends StatelessWidget {
  const HomeScrollableContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
